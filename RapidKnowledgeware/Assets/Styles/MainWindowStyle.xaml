<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">


    <!-- 直接使用颜色值，避免资源引用问题 -->
    <SolidColorBrush x:Key="WindowBackground" Color="#FFF5F7FA"/>
    <SolidColorBrush x:Key="PrimaryBlue" Color="#0078D7"/>
    <SolidColorBrush x:Key="LightBlue" Color="#E6F2FA"/>
    <SolidColorBrush x:Key="TextPrimary" Color="#262626"/>
    <SolidColorBrush x:Key="TextSecondary" Color="#595959"/>



    <!-- 标题栏样式 -->
    <Style x:Key="TitleBarStyle" TargetType="Border">
        <Setter Property="Background">
            <Setter.Value>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#FFFFFF" Offset="0"/>
                    <GradientStop Color="#F0F8FF" Offset="1"/>
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
        <Setter Property="BorderBrush" Value="#bfdbfe"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>


    <ControlTemplate TargetType="Button" x:Key="NewDialogueBtnStyle">
        <Border Name="back" 
                 Background="White"  CornerRadius="18" Margin="20 12">
            <Border.Effect>
                <DropShadowEffect Color="Gray" BlurRadius="10" ShadowDepth="0" Opacity="0.3" Direction="270"/>
            </Border.Effect>
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center"  Orientation="Horizontal">

                <Label Content="&#xe8a6;" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15"  Foreground="Black" FontFamily="{StaticResource iconfont}" Margin="0 1.3 0 0"/>
                <TextBlock Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="14"  Foreground="Black" Margin="0 0 10 0"/>

            </StackPanel>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightBlue}" TargetName="back"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}" TargetName="back"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D0E8FF" TargetName="back"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    
    <ControlTemplate TargetType="Button" x:Key="SendMessageBtn">
        <TextBlock Text="{TemplateBinding Content}" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="{TemplateBinding FontSize}"
                           FontFamily="{StaticResource iconfont}" Margin="0 1.3 0 0" Name="text" Foreground="{TemplateBinding Foreground}"/>
        <ControlTemplate.Triggers>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Foreground" Value="#b0c1ff" TargetName="text"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Foreground" Value="#b0c1ff" TargetName="text"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="#3964fe" TargetName="text"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <Style x:Key="SessionRadioButtonStyle" TargetType="RadioButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="RadioButton">
                    <Border x:Name="border" Background="Transparent" Margin="5 5" BorderBrush="Transparent" CornerRadius="7" BorderThickness="0" Padding="15,10 10 10">
                        <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Name="text"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="border" Property="Background" Value="#80bfdbfe"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="#903964fe"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="0,0,3,0"/>
                            <Setter TargetName="text" Property="TextElement.Foreground" Value="#3964fe"/>
                        </Trigger>
                        <!-- 仅在未选中时响应鼠标悬停 -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsMouseOver" Value="True"/>
                                <Condition Property="IsChecked" Value="False"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="border" Property="Background" Value="#F0F0F0"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <ControlTemplate TargetType="ToggleButton" x:Key="ToggleButtonTemplate">
        <Border Name="back" 
                Background="#55DAE3EA"
                BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="0" 
                CornerRadius="4">
            <ContentControl Content="{TemplateBinding Content}" 
                          FontFamily="{StaticResource iconfont}"  
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center"
                          Foreground="{StaticResource TextPrimary}"/>
        </Border>
        <ControlTemplate.Triggers>
            <!-- 按下状态 -->
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#D0E8FF" TargetName="back"/>
            </Trigger>
            <!-- 选中状态（优先级最高，放在最后） -->
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#D0E8FF" TargetName="back"/>
                <Setter Property="BorderBrush" Value="#3964fe" TargetName="back"/>
                <Setter Property="BorderThickness" Value="0 0 0 2" TargetName="back"/>
            </Trigger>
            <!-- 鼠标悬浮状态（仅当未选中时生效） -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#B0C4DE" TargetName="back"/>

            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <ControlTemplate TargetType="Button" x:Key="ButtonStyleKeyTemplate">
        <Border Name="back" 
                Background="Transparent" 
                BorderBrush="{TemplateBinding BorderBrush}" 
                BorderThickness="0" 
                CornerRadius="7">
            <ContentControl Content="{TemplateBinding Content}"  Margin="15 7 10 7"
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Left"
                          Foreground="{StaticResource TextPrimary}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource LightBlue}" TargetName="back"/>
                <Setter Property="BorderBrush" Value="{StaticResource PrimaryBlue}" TargetName="back"/>
            </Trigger>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Background" Value="#55DAE3EA" TargetName="back"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <ControlTemplate TargetType="Button" x:Key="OpenDebugBtn_Template">
        <Border
      Background="{TemplateBinding Background}"
      BorderBrush="{TemplateBinding BorderBrush}" 
      BorderThickness="{TemplateBinding BorderThickness}"
      CornerRadius="4">
            <ContentControl Content="{TemplateBinding Content}"   Name="text" 
                FontFamily="{StaticResource iconfont}"  
                VerticalAlignment="Center" 
                HorizontalAlignment="Center"
                Foreground="{StaticResource TextPrimary}"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsPressed" Value="True">
                <Setter Property="Foreground" Value="#D0E8FF" TargetName="text"/>
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate TargetType="ToggleButton" x:Key="DeepThinkingTBtn_Template">
        <Border Name="back" 
      Background="{TemplateBinding Background}"
      BorderBrush="{TemplateBinding BorderBrush}" 
      BorderThickness="{TemplateBinding BorderThickness}"
      CornerRadius="10">
            <StackPanel Orientation="Horizontal" >
                <Label Content="&#xe639;" Name="lable" FontFamily="{StaticResource iconfont}" FontSize="12" Margin="1 0 0 0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <TextBlock Text="{TemplateBinding Content}" Name="text" FontSize="{TemplateBinding FontSize}" VerticalAlignment="Center"   Margin="0 0 6 0" Foreground="{StaticResource TextPrimary}" FontFamily="Microsoft YaHei"/>

            </StackPanel>

        </Border>
        <ControlTemplate.Triggers>
            <!-- 选中状态（优先级最高，放在最后） -->
            <Trigger Property="IsChecked" Value="True">
                <Setter Property="Background" Value="#edf3fe" TargetName="back"/>
                <Setter Property="Foreground" Value="#3964fe" TargetName="text"/>
                <Setter Property="BorderBrush" Value="#b7c8fe" TargetName="back"/>
                <Setter Property="Foreground" Value="#3964fe" TargetName="lable"/>
            </Trigger>
            <!-- 鼠标悬浮状态（仅当未选中时生效） -->
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter Property="Background" Value="#EEE" TargetName="back"/>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


    <!-- （仅显示圆角滑块，轨道透明） -->
    <Style x:Key="SlimScrollBar" TargetType="ScrollBar">
        <Setter Property="Width" Value="6"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid>
                        <Track x:Name="PART_Track" IsDirectionReversed="true">
                            <Track.DecreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageUpCommand" 
                                              Background="Transparent" 
                                              BorderThickness="0"/>
                            </Track.DecreaseRepeatButton>
                            <Track.IncreaseRepeatButton>
                                <RepeatButton Command="ScrollBar.PageDownCommand" 
                                              Background="Transparent" 
                                              BorderThickness="0"/>
                            </Track.IncreaseRepeatButton>
                            <Track.Thumb>
                                <Thumb Background="#CCC" BorderThickness="0" Width="6">
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{TemplateBinding Background}" 
                                                    CornerRadius="3" 
                                                    Width="6"/>
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>


    <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="#94a3b8"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="Foreground" Value="#1e293b"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" 
              Background="Transparent"
              BorderBrush="{TemplateBinding BorderBrush}"
              BorderThickness="{TemplateBinding BorderThickness}"
              IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
              ClickMode="Press">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="4">
                                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="bd" Property="Background" Value="#55DAE3EA"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ToggleButton.Template>
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter x:Name="ContentPresenter"
                                              Content="{TemplateBinding SelectedItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              HorizontalAlignment="Left"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                                <Path x:Name="Arrow" 
                                  Data="M0,0 L4,4 L8,0" 
                                  Fill="#64748b"
                                  HorizontalAlignment="Right"
                                  VerticalAlignment="Center"
                                  Margin="0,0,0,0"
                                  Width="14" Height="6"
                                  StrokeThickness="0"/>
                            </StackPanel>
                        </ToggleButton>
                        <Popup x:Name="Popup" 
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Fade">
                            <Border x:Name="DropDownBorder"
                                Background="#ffffff"
                                BorderBrush="#e2e8f0"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,4,0,0">
                                <Border.Effect>
                                    <DropShadowEffect BlurRadius="8" ShadowDepth="2" Opacity="0.08" Color="#000000"/>
                                </Border.Effect>
                                <ScrollViewer>
                                    <ItemsPresenter/>
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsDropDownOpen" Value="True">
                            <Setter TargetName="Arrow" Property="Fill" Value="#3b82f6"/>
                            <Setter Property="BorderBrush" Value="#3b82f6" TargetName="ToggleButton"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="ToggleButton" Property="Background" Value="#55DAE3EA"/>
                            
                            <Setter Property="BorderBrush" Value="#60a5fa" TargetName="ToggleButton"/>
                            <Setter TargetName="Arrow" Property="Fill" Value="#60a5fa"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <!-- 下拉项容器样式 -->
        <Setter Property="ItemContainerStyle">
            <Setter.Value>
                <Style TargetType="ComboBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="Foreground" Value="#334155"/>
                    <Setter Property="FontSize" Value="13"/>
                    <Setter Property="Padding" Value="10,6"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="border" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2">
                                    <ContentPresenter/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#f1f5f9"/>
                                    </Trigger>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="border" Property="Background" Value="#dbeafe"/>
                                        <Setter Property="Foreground" Value="#1e40af"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Setter.Value>
        </Setter>
    </Style>


    <!-- 分段式 RadioButton（用于类型切换） -->
    <ControlTemplate x:Key="SegmentRadioButtonTemplate" TargetType="RadioButton">
        <Border x:Name="border" Background="#e2e8f0" CornerRadius="8" Padding="10,4" Cursor="Hand">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="border" Property="Background" Value="#bfdbfe"/>
                <Setter Property="FontWeight" Value="Bold"/>
            </Trigger>
            <MultiTrigger>
                <MultiTrigger.Conditions>
                    <Condition Property="IsMouseOver" Value="True"/>
                    <Condition Property="IsChecked" Value="False"/>
                </MultiTrigger.Conditions>
                <Setter TargetName="border" Property="Background" Value="#f1f5f9"/>
            </MultiTrigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>


</ResourceDictionary>