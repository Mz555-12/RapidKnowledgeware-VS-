<UserControl x:Class="RapidKnowledgeware.Views.HelperView.KnowledgeBaseFuncView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RapidKnowledgeware.Views.HelperView"
             mc:Ignorable="d" 
             d:DesignHeight="110" d:DesignWidth="748">
    <Grid Background="White">
        <StackPanel Grid.Row="1" Margin="0 0 10 0" >

            <Grid  Margin="10 25 10 0">
                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Padding="10">
                    <StackPanel Orientation="Horizontal">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <Label Content="当前嵌入模型："/>
                                    <TextBox Text="{Binding KnowledgeBaseModel.CurrentEmbeddingName, UpdateSourceTrigger=PropertyChanged}"    Margin="0 0 30 0" MinWidth="150" MaxWidth="120" BorderThickness="0 0 0 1" Padding="10 0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <Label Content="相似度阈值："/>
                                    <TextBox Text="{Binding KnowledgeBaseModel.IndexSimilarityThreshold, UpdateSourceTrigger=LostFocus}"   
                       Margin="0 0 30 0" MinWidth="150" MaxWidth="120" BorderThickness="0 0 0 1" Padding="10 0" 
                       VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                            <StackPanel Orientation="Horizontal">
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <Label Content="检索数量："/>
                                    <TextBox Text="{Binding KnowledgeBaseModel.SearchQuantity, UpdateSourceTrigger=LostFocus}"   
                       Margin="0 0 30 0" MinWidth="150" MaxWidth="150" BorderThickness="0 0 0 1" Padding="10 0" 
                       VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Left">
                                    <!--多个分隔符请用英文逗号分隔，例如：###,##-->
                                    <Label Content="分块规则："/>
                                    <TextBox Text="{Binding KnowledgeBaseModel.BlockRule, UpdateSourceTrigger=PropertyChanged}"    Margin="0 0 30 0" MinWidth="150" MaxWidth="170" BorderThickness="0 0 0 1" Padding="10 0" VerticalContentAlignment="Center" HorizontalContentAlignment="Center" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                        <Button Content="添加文件" Margin="10 0 20 0" Height="30" Width="100" HorizontalAlignment="Right"
Command="{Binding AddKnowledgeCommand}"/>
                    </StackPanel>


                </Border>

                <Label Content="知识库设置" Margin="20 -10 0 0" FontSize="14" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="15 0" Background="White"  Foreground="#666"/>

            </Grid>

        </StackPanel>
    </Grid>
</UserControl>
